flutter clean
flutter pub get
flutter run

React                         Flutter

useEffect(..., [])        →   initState()

useEffect(..., [value])   →   didUpdateWidget()

cleanup                   →   dispose()